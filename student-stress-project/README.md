# Student Stress Project

A starter full-stack student stress assessment and recovery recommendation project.

## Run frontend
Open `frontend/index.html` in a browser.

## Run backend
```bash
pip install -r requirements.txt
uvicorn backend.main:app --reload
```

The ML and deep-learning folders contain starter pipelines/placeholders that can be extended with a real dataset and trained models.
